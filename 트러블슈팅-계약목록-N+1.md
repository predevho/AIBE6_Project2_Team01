# 계약이 쌓일수록 느려지는 목록 화면, 3N+4 쿼리를 6개로 줄인 이야기

> **TL;DR**
> - 모델 계약 목록에서 계약 1건당 공고·유저·기업을 개별 조회 → `3N+4` 쿼리
> - id를 모아 `IN` 배치 조회 + `Map` 매칭으로 **상수 6개**로 축소 (실측: 계약 100건 기준 304개 → 6개)
> - `@EntityGraph`는 못 씀 — 엔티티가 `@ManyToOne`이 아니라 `Long` id 참조라 걸 곳이 없다

모델 마이페이지의 "계약 내역" 탭이 유독 느렸다. 이상하게도 **같은 API인데 기업 계정으로 보면 멀쩡**했다. 이 차이가 실마리였다.

## 사실은 N+1이 아니라 "3N+4"

SQL 로그를 켜보니 계약 건수만큼 쿼리가 따라 늘었다. 목록을 순회하며 **한 건마다 DB를 세 번씩** 두드리고 있었다.

```java
return applications.stream()
        .filter(...)
        .map(application -> {
            JobPostingResponse jobPosting = jobPostingService.getJobPosting(application.getJobPostingId()); // ① 공고
            User clientUser = userService.findById(jobPosting.clientId());                                   // ② 유저
            var client = clientService.findByUserId(clientUser.getId());                                     // ③ 기업
            return toContractListItemResponse(contract, application, client.getCompanyName());
        })
        .toList();
```

계약 `N`건이면 고정 조회 4번(model·client·applications·contracts)에 건당 3번이 더해져 **3N+4**. 게다가 프론트의 필터 버튼(진행중/완료/취소)은 누를 때마다 재요청이라, 탭 몇 번 오가면 쿼리가 수백 개씩 나갔다.

그런데 ②는 아예 낭비였다. `JobPosting.clientId`가 이미 기업의 user id인데, 그걸로 User를 조회해 돌려받은 `clientUser.getId()`를 다시 기업 조회에 넘긴다 — **넣은 값을 그대로 돌려받으려 DB를 한 번 더 치는 줄**이었다.

## 정답은 바로 옆 메서드에 있었다

"왜 기업 계정은 멀쩡한가"의 답이 여기 있었다. 같은 클래스의 `getClientContracts`는 애초에 **필요한 id를 모아 한 번에 조회하고 Map으로 매칭**하도록 짜여 있었다. 이 패턴을 모델 경로에도 그대로 이식했다.

```java
// 1) 공고 배치 조회: jobPostingId -> clientId(기업 user id)
Map<Long, Long> clientUserIdByJobPostingId = jobPostingService.getJobPostingsByIds(jobPostingIds).stream()
        .collect(Collectors.toMap(JobPostingResponse::id, JobPostingResponse::clientId));

// 2) 기업 배치 조회: clientUserId -> companyName
Map<Long, String> companyNameByUserId = clientService.findByUserIds(clientUserIds).stream()
        .collect(Collectors.toMap(c -> c.getUser().getId(), Client::getCompanyName));

// 3) 스트림에서는 DB 대신 Map만 조회
return targets.stream()
        .map(a -> toContractListItemResponse(
                contractMap.get(a.getId()),
                a,
                companyNameByUserId.get(clientUserIdByJobPostingId.get(a.getJobPostingId()))))
        .toList();
```

낭비였던 `userService.findById` 호출은 통째로 지웠다.

## "그런데 @EntityGraph를 쓰지 그랬어?"

먼저 떠올린 건 맞다. 그런데 못 썼다. 우리 프로젝트는 도메인 결합을 끊으려고 엔티티를 `@ManyToOne`으로 엮지 않고 **`Long` id 값으로만 참조**한다(`Application`은 `Long jobPostingId`). `@EntityGraph`·`@BatchSize`·fetch join은 전부 **JPA 연관관계 위에서만 동작**하는데, 우리한테는 숫자 컬럼뿐이라 묶어서 당겨올 연관 자체가 없다. 게다가 이번 N+1은 lazy 로딩이 아니라 **루프 안에서 `service.findById()`를 직접 반복 호출**해 난 것이라, 그 어노테이션들이 손댈 수 있는 종류도 아니었다. id 참조 설계에선 **배치+Map이 대체재가 아니라 정공법**이다.

## 예외를 던질까, null을 둘까

배치로 바꾸니 없는 데이터가 예외 대신 `null`이 된다. 단건 조회면 예외가 맞지만, **목록**에서 예외를 던지면 계약 하나 때문에 화면 전체가 안 열린다. 실제로 공고는 하드 삭제가 가능해 "절대 없을 수 없는 상태"도 아니고, 코드베이스의 다른 배치 목록도 이미 null을 허용하고 있어서 그대로 뒀다.

## 결과 (이론 + 실측)

| 조회 | 변경 전 | 변경 후 |
| --- | --- | --- |
| 공고(`job_posting`) | `id=?` 개별 N번 | `id in (…)` **1번** |
| 기업(`client`) | `user_id=?` 개별 N번 | `id in (…)` **1번** |
| 유저 | 서로 다른 기업 수만큼 (최대 N번, 낭비) | **제거** |
| 고정(model·client·applications·contracts) | 4번 | 4번 |
| 합계 | **3N + 4** (최대) | **6개 상수** |

이론만 믿기 싫어 Hibernate `StatementInspector`로 실행 SQL을 한 줄씩 세는 테스트([`ContractQueryCountTest`](modle_backend/src/test/java/com/modle/querycount/ContractQueryCountTest.java))를 붙여, 리팩터링 전/후 커밋을 각각 체크아웃해 돌렸다. 매 호출 전 영속성 컨텍스트를 비워 실제 HTTP 요청 1건과 같은 조건으로 맞췄다.

| 계약 수 | 변경 전 (계약마다 기업이 다름) | 변경 전 (기업 하나) | 변경 후 |
| --- | --- | --- | --- |
| 1 | 7개 | 7개 | **6개** |
| 10 | 34개 | 25개 | **6개** |
| 100 | **304개** | 205개 | **6개** |

![변경 전/후 쿼리 수·응답 시간](측정-계약목록-N+1/차트-쿼리수-응답시간.png)

계약 100건이면 정확히 `3×100+4 = 304`개가 나갔고, 변경 후엔 몇 건이든 6개였다. 실행된 SQL 전문과 측정 방법은 [`측정-계약목록-N+1/`](측정-계약목록-N+1/README.md)에 남겼다. 손으로 셀 때 놓친 게 하나 있었는데, `User.client`가 `@OneToOne(mappedBy)`라 LAZY가 안 걸려서 **모델의 유저를 불러오는 순간 `client where user_id=?`가 한 번 더 나간다**. 계약 코드가 아니라 유저 엔티티 매핑에서 오는 것이라 변경 전후 똑같이 1개씩 있고, 그래서 고정 조회가 3이 아니라 4다. 유저 조회(②)는 같은 기업이 반복되면 1차 캐시가 흡수해서 기업이 전부 다를 때만 N번(최악 3N+4), 기업이 하나면 1번(최선 2N+5)이다. 반면 `clientRepository.findByUserId`는 파생 쿼리라 1차 캐시가 안 먹어 **항상** N번 나간다. 응답 시간은 in-memory H2 기준 100건에서 **107ms → 11ms**였고, 네트워크 왕복이 있는 실제 MySQL에선 쿼리 개수 차이가 그대로 응답 시간 차이로 커진다.

## 배운 것

- N+1이 의심되면 **루프 안의 조회**부터 본다. `.map()`/`for` 안에서 repository·service를 부르면 십중팔구 범인.
- 문제를 풀기 전에 **비슷한 걸 이미 잘 처리한 코드**를 먼저 찾자. 이번 정답은 바로 옆 메서드에 있었다.
- "방어적"의 방향은 맥락마다 다르다. 단건 조회의 fail-fast가 목록에선 화면을 통째로 깨는 fail-loud가 된다.

> 커밋: [`[Refactor] 모델 계약 목록 조회 N+1 쿼리 제거`](https://github.com/predevho/AIBE6_Project2_Team01/commit/045ec7bd955ce8f2a54222554d5ed9d549fe0f3f)
